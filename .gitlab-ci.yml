pages:
  image: fedora:latest
  script:
    - sudo dnf install -y unzip
    - mkdir public
    - mv index.html public/
    - mv doc public/
    - mv style.css public/
    - ./doc.sh
  artifacts:
    paths:
      - public
  only:
    - pages
