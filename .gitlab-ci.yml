pages:
  script:
    - sudo dnf install -y curl unzip
    - mkdir public
    - mv index.html public/
    - mv doc public/
    - mv style.css public/
    - ./doc.sh
  artifacts:
    paths:
      - public
  only:
    - pages
