pages:
  image: registry.gitlab.gnome.org/gnome/gnome-runtime-images/gnome:master
  script:
    - ./doc.sh
    - mv index.html public/
    - mv style.css public/
  artifacts:
    paths:
      - public
  only:
    - pages
